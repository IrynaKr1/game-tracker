const GAME_STATUS = {
  NOT_STARTED: 'Not started',
  IN_PROGRESS: 'In progress',
  COMPLETED: 'Completed',
};

const GAME_STATUS_LIST = Object.values(GAME_STATUS);

module.exports = { GAME_STATUS, GAME_STATUS_LIST };
